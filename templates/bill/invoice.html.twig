<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
    </head>
    <body>
        <div class="container">
            <div class="">
                <h2>Détail du Devis</h2>
                <p>Adresse de livraison: {{ quotation.getDeliveryAddress }}</p>
                <p>Adresse du devis: {{ quotation.getBillingAddress }}</p>
                <p>Date de demande: {{ quotation.requestDate|date("m/d/Y") }}</p>
                <p>Date de confirmation: {{ quotation.acceptanceDate|date("m/d/Y") }}</p>
                <p>Approuvé ?: {{ quotation.approved }}</p>
                <p>Companie: {{ quotation.getCompany }}</p>
                <p>Email: {{ quotation.email }}</p>
            </div>
        </div>
    </body>
</html>